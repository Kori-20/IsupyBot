# IsupyBot Configuration File
# Lines starting with # are comments and will be ignored
# Format: key=value (no spaces around the = sign)
# !!! ip_address && channel_id need to be filled by default

# Server connection settings
ip_address=
port=25565
query=true

# Discord settings
channel_id=
show_player_names=true

# Monitoring frequency (1-5)
# 1: 1 minute, 2: 5 minutes, 3: 10 minutes, 4: 1 hour, 5: 6 hours
frequency_level=2
